import { BarberShopService } from "@prisma/client";

interface ServiceItemProps {
    service: BarberShopService
}

const ServicerItem= ({service}: ServiceItemProps) => {
    return ( {barber} );
}
 
interface ServiceItemProps {
    service: BarberShopService
}export 

default ServicerItem